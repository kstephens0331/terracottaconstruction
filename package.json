{
  "name": "terracottaconstruction-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd terracotta-seo-website && npm install && npm run build",
    "dev": "cd terracotta-seo-website && npm run dev"
  }
}
